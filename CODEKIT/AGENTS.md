# Agent Guide

## Required Workflow

READ -> UNDERSTAND -> CHANGE -> TEST -> DOCUMENT -> REPORT.

Before starting work, read [PROJECT_MEMORY.md](PROJECT_MEMORY.md), this file, the relevant entries in [DEVLOG.md](DEVLOG.md), and [LANGUAGE.md](LANGUAGE.md). Follow [DOCUMENTATION_RULES.md](DOCUMENTATION_RULES.md). Inspect the actual code related to the task before editing it. Code and configuration are the source of truth; this guide is an orientation aid.

Never invent facts, history, design rationale, commands, or roadmap commitments. Mark missing evidence **Unknown** or **Not confirmed yet**. Label inferences **Likely:**. Never put credentials or secret values in documentation.

## Project Identity

- Working label and product name: **RADAR**, confirmed from the page `<title>RADAR // ACTIVE</title>` and the on-page heading `Radar.` in `index.html`.
- Project root: the repository root (the folder containing this `CODEKIT/` subfolder, `index.html`, and `NothingHere`).
- Project type: a single-page, client-only peer-to-peer (P2P) file transfer web app. Two browsers connect directly to each other over WebRTC and send a file straight from one device to the other.
- Deployed address: **https://radatit.pages.dev** (owner-stated, Cloudflare Pages). Not independently verified by fetching the URL from this workspace.
- Observed stage on **2026-09-13**: a single working HTML file implementing the full app (UI, styling, and logic together). No backend server code, build step, or test suite is present in this repository.
- Product maturity and prior development history outside this repository: **Unknown**.

## Confirmed Baseline

As of **2026-09-13**, the repository root contains:

- `index.html` — the entire RADAR application (markup, CSS, and JavaScript in one file).
- `NothingHere` — a 1-byte, effectively empty file at the repo root. Purpose **Unknown**; not referenced anywhere in `index.html`.
- `CODEKIT/` — this documentation kit (seven Markdown files plus its own placeholder `NothingHere`).

The repository is a Git repository with remote `origin` at `https://github.com/bwinwho/radar`. Commit history visible in this workspace (oldest first): `Create NothingHere`, `Add files via upload`, `Rename gemini-code-1789323854310.html to index.html`, `Update index.html`. The renamed filename (`gemini-code-...html`) suggests the original file was generated by a tool named Gemini; this is **Likely:**, not confirmed by any in-repo evidence beyond the filename.

## Technology Stack

| Area | Evidence / status |
| --- | --- |
| Application language | Vanilla JavaScript (ES6+), inline in `index.html`. No framework. |
| Styling | Inline `<style>` block in `index.html`; no CSS framework or preprocessor. |
| P2P / networking | [PeerJS](https://peerjs.com/) `1.5.2`, loaded from `https://unpkg.com/peerjs@1.5.2/dist/peerjs.min.js`. PeerJS wraps WebRTC `RTCDataChannel` and by default brokers connections through PeerJS's public cloud signaling server (`0.peerjs.com`) unless configured otherwise. `index.html` does not pass custom PeerServer options, so the default public broker is **Likely:** in use. |
| Persistence | Browser `localStorage`, key `radar_recent` (array of up to 3 recent peer IDs). |
| Runtime / package manager | None. No `package.json`, no lockfile, no `node_modules`. The app runs as static HTML with a CDN script tag — no build or install step. |
| Hosting | Cloudflare Pages, at `radatit.pages.dev` (owner-stated). No Cloudflare/Pages configuration files (`wrangler.toml`, `_headers`, `_redirects`) are present in this repository. |
| Build, lint, packaging, testing tools | None found. There is nothing to build or lint; the static file is deployed as-is. |
| Documentation format | Markdown (`.md`), under `CODEKIT/`. |

Do not select a stack or infer installed project dependencies from the tools available on an agent's machine.

## Architecture and Entry Points

Single entry point: `index.html`. There is no router, no server, no database, and no authentication.

High-level flow, traced from the code:

1. On load, the page generates a random human-readable callsign (`WORD-NN`, e.g. `NEON-42`) client-side and creates a PeerJS `Peer` using that callsign as the peer ID (`index.html:118-128`).
2. `loadRecentPeers()` reads `localStorage.radar_recent` and renders up to 3 quick-connect buttons (`index.html:295-308`).
3. The local user either waits for an incoming connection (`peer.on('connection', ...)`, `index.html:150-153`) or types a target callsign and clicks "Establish Link", which calls `peer.connect(peerId)` (`index.html:155-165`).
4. Once a `DataConnection` opens (`conn.on('open')`), the UI swaps from the connect panel to the transfer panel and the connected peer's ID is saved to `localStorage` via `saveRecentPeer()` (`index.html:173-180`).
5. Sending a file: `fileInput` change handler reads the chosen file in `256 KB` chunks with `FileReader`, sends a `{type:'header', filename, size}` message, then streams `{type:'chunk', chunk}` messages, checking `conn.dataChannel.bufferedAmount` and delaying with `setTimeout` when it exceeds `16 MB` (basic backpressure), and finally sends `{type:'eof'}` (`index.html:212-256`).
6. Receiving a file: `conn.on('data', ...)` accumulates chunks into `receiveBuffer`, and on `eof` assembles a `Blob`, creates an object URL, and triggers a download via a synthetic `<a download>` click (`index.html:182-210`).
7. `updateTelemetryUI()` computes instantaneous KBPS/MBPS and progress-bar percentage from elapsed time and bytes transferred so far, on both sender and receiver (`index.html:259-275`).

There is no server-side code in this repository; the only network dependency is the PeerJS signaling connection used to establish the WebRTC link, after which the file data flows directly peer-to-peer.

## Repository Map

| Path | Responsibility |
| --- | --- |
| `index.html` | The entire RADAR application: markup, styling, and P2P/file-transfer logic. |
| `NothingHere` | Empty placeholder file at repo root. Purpose **Unknown**. |
| `CODEKIT/LANGUAGE.md` | Communication and teaching preferences. |
| `CODEKIT/AGENTS.md` | Technical orientation and instructions for agents (this file). |
| `CODEKIT/README.md` | Human-friendly project explanation. |
| `CODEKIT/MARKET.md` | Evidence-based product story. |
| `CODEKIT/DEVLOG.md` | Append-only history, findings, and decisions. |
| `CODEKIT/PROJECT_MEMORY.md` | Concise current briefing. |
| `CODEKIT/DOCUMENTATION_RULES.md` | Maintenance and verification rules. |
| `CODEKIT/NothingHere` | Empty placeholder file inside the kit. Purpose **Unknown**. |

## Major Modules and Data Model

There are no separate modules or files beyond `index.html`; all logic is inline `<script>` code organized into top-level functions:

- `generateId()` — random callsign generator (word list + 2-digit number).
- `setupConnection(connectedId)` — wires up `conn.on('open')` and `conn.on('data')` handlers for a given `DataConnection`.
- `initiateConnection(peerId)` — starts an outbound `peer.connect()`.
- `updateTelemetryUI(currentBytes, totalBytes, startTime)` / `resetTelemetryUI()` — transfer speed/progress display.
- `saveRecentPeer(id)` / `loadRecentPeers()` — `localStorage`-backed recent-peers list.

Data model: no database. The only persisted state is `localStorage['radar_recent']`, a JSON array of up to 3 peer-ID strings. In-memory transfer state (`receiveBuffer`, `receiveMeta`, `receiveBytes`, `receiveStartTime`) lives in page-global variables and is not persisted.

Wire protocol between peers (over the PeerJS `DataConnection`, all plain JS objects sent via `conn.send()`):

- `{ type: 'header', filename, size }` — sent once before a file transfer starts.
- `{ type: 'chunk', chunk: ArrayBuffer }` — one per 256 KB slice of the file.
- `{ type: 'eof' }` — sent after the last chunk.

## Important Flows

- **Connecting:** covered under Architecture above. No login/auth exists; the "identity" is just a randomly generated, unauthenticated callsign string.
- **File transfer (send/receive):** covered under Architecture above; this is the app's core feature.
- **Reconnecting to a recent peer:** clicking a button in the "Recent Targets" list calls `initiateConnection(id)` with a previously seen peer ID (`index.html:300-305`).
- Navigation, payments, and search: not applicable — none exist in this app.

## External Services and Configuration

- **PeerJS cloud broker:** the app depends on PeerJS's default signaling infrastructure (loaded from the `unpkg` CDN, connecting to PeerJS's public cloud server) to establish WebRTC connections. No self-hosted PeerServer or custom ICE/STUN/TURN configuration is present in `index.html`.
- **Cloudflare Pages:** stated hosting target, `radatit.pages.dev`. No Pages-specific config files are present in the repo to confirm build/deploy settings from code.
- No environment variables, API keys, or secrets are referenced anywhere in `index.html` or `CODEKIT/`.

## Running and Deploying

- **Local run:** `index.html` is a static file with no build step. Opening it directly in a browser, or serving the repo root with any static file server, is sufficient to run the app locally. (Not executed from this workspace as part of documentation; stated as an inference from the file's self-contained structure, **Likely:**.)
- **Deployment:** the owner states the app is deployed to Cloudflare Pages at `radatit.pages.dev`. No `wrangler.toml`, Pages build settings, or CI/CD workflow files exist in this repository to confirm exactly how deploys are triggered (e.g., automatic deploy from the `main` branch is **Likely:** given Cloudflare Pages' default Git integration, but not confirmed from files in this repo).
- No package manager, install command, lint command, or test command exists for this project.

## Conventions and Decisions

- The app is intentionally a single self-contained HTML file with inline CSS/JS and one CDN script dependency (PeerJS) — no build tooling.
- UI copy uses a "telemetry / military radar" theme (callsigns, "uplink", "transmission", monospace status line) consistently throughout `index.html`.
- Chunked transfer uses a fixed `256 KB` chunk size and a `16 MB` `bufferedAmount` backpressure threshold (`index.html:222, 237`); these are hardcoded constants, not configurable.
- The owner requested a permanent seven-file `CODEKIT/` documentation kit; meaningful code work must include the relevant documentation updates in the same task (see [DOCUMENTATION_RULES.md](DOCUMENTATION_RULES.md)).

## Constraints and Fragile Areas

- Treat the repository root (not `CODEKIT/`) as the project root.
- Because signaling relies on PeerJS's public default server, connectivity depends on that third-party service's availability and is outside this repo's control.
- WebRTC data channels (and thus this app) may fail to connect across some restrictive NATs/firewalls, since no custom TURN server is configured — plain PeerJS defaults only include STUN, which does not guarantee connectivity in all network conditions. **Likely:**, based on standard WebRTC/PeerJS behavior; not something this workspace can test.
- The receive path buffers the entire incoming file in memory (`receiveBuffer` array of chunks) before assembling the `Blob` — very large files could be memory-intensive on the receiving device. Not yet confirmed as a bug; no size limit or streaming-to-disk exists in the code.
- `NothingHere` (both at repo root and inside `CODEKIT/`) has unknown purpose; do not delete without checking with the owner, since intent behind it isn't documented anywhere.

## Known Problems and Testing

No automated tests or test configuration exist in this repository. No confirmed application bugs have been identified through code inspection alone; this is not evidence the app is bug-free, since it has not been run or tested from this workspace. The potential large-file memory concern noted above is a code-review observation, not a confirmed bug.

## Agent Handoff

- Current work: CODEKIT was updated on **2026-09-13** to describe the actual RADAR app now present in the repository, replacing the earlier "empty workspace" baseline recorded on 2026-09-12.
- Completed: read `index.html` in full, confirmed repo structure and Git history, rewrote all seven CODEKIT files with evidence-based content.
- Next logical work: confirm the Cloudflare Pages deployment configuration, and decide what to do with the two unexplained `NothingHere` files. Beyond that, next steps depend on what the owner wants to build on RADAR next — **Not confirmed yet**.
- Caution: do not assume backend/server capability exists — this is a fully client-side app that depends on a third-party signaling service for peer discovery.
